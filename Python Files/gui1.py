# Simple GUI script (My first.)
from Tkinter import *

# creates the window
root = Tk()

#modify root window
root.title("First GUI") # title
root.geometry("200x100") # dimensions of the window

# kick off the event loop
root.mainloop()
